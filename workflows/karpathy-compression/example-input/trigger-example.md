# Example Input — Karpathy Compression / Curriculum Workflow

## Trigger

Run `karpathy-compression` against fictional demo data.

## Input surface

A fictional candidate file plus experiment objective such as shorten, clarify, split, or refactor.

## Runtime declaration

```json
{
  "runtime": "example-runtime",
  "repo_root": "<repo-root>",
  "available_capabilities": ["filesystem_read", "filesystem_write", "agent_dispatch"]
}
```
