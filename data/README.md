# Data Layer

This folder demonstrates the **local analytical store** pattern.

The public repo includes schema, seed data, and example queries. It does not include a real database file. A user can generate a local demo database from the text files under `data/duckdb/`.
