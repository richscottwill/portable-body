# Tools

This public repo contains guidance, not live automation credentials.

The private working layer can have an export command that generates this repo from templates, but the export must remain allowlist-based and scan-gated.
